#-------------------------------------------------------------------------------
# Significance function 
#-------------------------------------------------------------------------------
sig_star <- function(x){ifelse(x <= 0.001, "***",
                     ifelse(x <= 0.01, "**" ,
                            ifelse(x <= 0.05, "*", "")))}
 